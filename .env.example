# Vite Configuration Example
# Copy this file to .env.local and customize as needed

# Custom hostname for HMR (Hot Module Reload) WebSocket connection
# Useful for Tailscale, local network access, or custom domains
# Example: VITE_CUSTOM_HOST=myserver.tail08f20.ts.net
# VITE_CUSTOM_HOST=localhost

# Allowed hosts (comma-separated) for additional security
# Leave empty or unset to allow all hosts (recommended for local development)
# Example: VITE_ALLOWED_HOSTS=localhost,myserver.tail08f20.ts.net,192.168.1.100
# VITE_ALLOWED_HOSTS=
